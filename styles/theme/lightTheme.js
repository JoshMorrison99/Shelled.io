import { createTheme } from "@mui/material/styles";

const lightTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#42403f",
      background_website: "#f6f6f6",
      background_accordian: "#fefeff",
    },
  },
});

export default lightTheme;
